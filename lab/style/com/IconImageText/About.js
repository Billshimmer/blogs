import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  containerVertical:{ //上下样式时使用的外部容器

  },
  containerHorizontal:{ //左右样式时使用的外部容器
  },
  image: {
    height:80,
    width:76,
    borderRadius:4
  },
  text: {
    fontSize:Theme.fontSizeSmall,
    color:Theme.secondaryTextColor
  },
});
