import React, {
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  container: {
    height:44,
    width:100,
    backgroundColor:'gray'
  },
  badgePosition: {
    top: 0,
    right: 0,
  }

});
