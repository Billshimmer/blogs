import ReactNative, {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  container: {
    //alignSelf:'flex-start',
    backgroundColor:'black',
    paddingHorizontal:5,
    borderRadius:10,
    width:100,
  },
  text: {
    color:'white',
    fontSize:20,
  },

});
