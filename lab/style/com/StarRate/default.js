import ReactNative, {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  itemView:{
    //用于设置item之间margin
  },
  image: {
    height:30,
    width:30,
  },
  icon: {
    fontSize:30,
  },
  defaultText: {
    fontSize:18,
  }
});
