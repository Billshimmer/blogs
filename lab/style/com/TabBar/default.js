import React, { 
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  // container: {
  //   //TODO
  // },
  line: {
    backgroundColor: Theme.primaryColor,
  }
});
