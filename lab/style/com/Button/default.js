import React, {
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';
export default StyleSheet.create({
  button: {
    backgroundColor: Theme.primaryColor,
    borderColor: 'rgba(0, 0, 0, 0.2)',
    borderWidth: 1,
    borderRadius: 5,
    height:44,
  },
  text: {
    color: '#fff',
    fontSize: 18,
  },
});
