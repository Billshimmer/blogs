import React, { 
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';
export default StyleSheet.create({
  button: {
    backgroundColor: "#FBFAFC",
    borderColor: Theme.darkPrimaryColor,
    borderWidth: 1,
    borderRadius: 5,
    height:44,
  },
  text: {
    color: '#000',
    fontSize:18,
  },
});
