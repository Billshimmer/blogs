import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  button: {
    height:44,
    marginHorizontal: 12,
    backgroundColor: Theme.primaryColor,
  },
  text: {
    color: '#FFFFFF',
    fontSize: 16,
  }
});
