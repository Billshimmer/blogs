import React, { 
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';
export default StyleSheet.create({
  text:{
    fontSize:12,
    color:'#ccc',
    paddingTop:8,
    textAlign:'center',
    marginBottom:8,
    alignSelf:'center',
  },
});
