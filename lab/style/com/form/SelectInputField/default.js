import React, {
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';
export default StyleSheet.create({
  rightIcon:{
    color:Theme.secondaryTextColor,
    fontSize:15,
    paddingLeft:10,
  }
});
