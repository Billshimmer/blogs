import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  // fieldContainer: {
  //
  // },
  textInput: {
    height: 44,
    flex: 1,
    fontSize: 16,
    color: Theme.fontColor,
  },
});
