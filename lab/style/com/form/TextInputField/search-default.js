import React, {
  Component,
  PropTypes,
} from 'react';
import ReactNative, {
  StyleSheet,
} from 'react-native';
export default StyleSheet.create({
  // fieldContainer: {
  //
  // },
  textInput: {
    height: 44,
    flex: 1,
    fontSize:14,
    color: Theme.primaryTextColor,
  },
});
